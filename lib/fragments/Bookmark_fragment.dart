
import 'package:flutter/material.dart';

class BookmarkFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("Bookmark fragment"),
    );
  }

}